language: r
sudo: required
dist: trusty

addons:
  apt:
    packages:
    - libproj-dev
    - libgdal-dev

r_github_packages:
  - jimhester/lintr
  - jimhester/covr

after_success:
  - Rscript -e 'covr::codecov()'
